# Copy to .env and adjust for a real deployment. These defaults match the
# committed docker-compose.yml and are local/evaluation only — never reuse
# them, or the SECRET_KEY below, outside local dev.
DATABASE_URL=postgresql+psycopg2://factory:factory@db:5432/factory_erp
SECRET_KEY=dev-only-change-me
