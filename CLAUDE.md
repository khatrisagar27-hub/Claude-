# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This is a newly initialized repository with no source code, build configuration, or tooling set up yet. There are no established conventions, workflows, or architecture to document at this time.

Once the project is bootstrapped, update this file with:

- **Build & test commands** — how to install dependencies, run tests (including single-test commands), lint, and build
- **Architecture overview** — high-level structure that requires reading multiple files to understand (data flow, module boundaries, key abstractions)
- **Project-specific conventions** — naming patterns, code organization rules, anything non-obvious that a new contributor would need to know
